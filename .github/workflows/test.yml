---

name: test

'on':
  push:
  schedule:
    - cron: '0 0 * * 2'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6.0.2
        with:
          persist-credentials: false

      - uses: actions/setup-python@v5.3.0
        with:
          python-version: '3.13'
          cache: pip

      - uses: extractions/setup-just@v3

      - run: pip install -r requirements-dev.txt

      - run: just test -v

      - run: just type

      - run: just lint --exit-zero


  update-manual:
    needs: [test]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6.0.2

      - uses: actions/setup-python@v5.3.0
        with:
          python-version: '3.13'
          cache: pip

      - uses: extractions/setup-just@v3

      - run: pip install -r requirements.txt

      - name: re-generate cli output examples in readme file
        run: just update-manual

      - name: see if something changed
        id: changed
        run: |
          chg=$(git diff --shortstat | wc -l)
          echo "changed=${chg}" >> ${GITHUB_OUTPUT}

      - name: show changes
        if: steps.changed.outputs.changed != '0'
        run: git diff

      - name: commit changes
        if: steps.changed.outputs.changed != '0'
        env:
          GIT_MAIL: >-
            41898282+github-actions[bot]@users.noreply.github.com
        run: |
          git config user.email "${GIT_MAIL}"
          git config user.name "github-actions[bot]"
          git add readme.md
          git commit -m ":memo: update usage instructions && examples"
          git push

      - name: print env
        run: env | sort

      - name: list supported fonts
        run: fc-list ':charset=2800' file spacing family

      - name: list supported monospace fonts
        run: fc-list ':charset=2800 :mono' file

      - name: ':memo: update screenshot'
        run: just take-screenshot

...
